// 마이그레이션 찾아보기
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model Article {
  id           String   @id(map: "Blog_pkey")
  title        String
  contents     String
  date         DateTime @default(now())
  categoryId   String
  thumbnailId  String?
  thumbnailUrl String?
  Category     Category @relation(fields: [categoryId], references: [id], map: "Blog_categoryId_fkey")
}

model Category {
  id      String    @id
  name    String
  Article Article[]
}
